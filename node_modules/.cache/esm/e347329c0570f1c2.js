let express,mongoose,cors,readdirSync;_057‍.w("express",[["default",["express"],function(v){express=v}]]);_057‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_057‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_057‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);



const morgan = require("morgan");
// config dotenv file
require("dotenv").config();
//rest obj
const app = express();

// server setupe
mongoose
  .connect(process.env.MONGODB_URI, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
  })
  .then(() => console.log("Connected TO DB"))
  .catch((err) => _057‍.g.console.log(err));

//middlewares
app.use(express.json());
app.use(cors());
app.use(morgan("dev"));

//route
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

// app.use("/api", require("./routes/authRoutes"));

app.get("/", (req, res) => {
  res.send("<h1>Welcome to Techinfoyt</h1>");
});

//listen to port
const PORT = process.env.PORT;

app.listen(PORT, () => {
  console.log(`Server Running On Port ${PORT} `);
});
